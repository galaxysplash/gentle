#include <cstdio>
#include <cstdlib>
#include <filesystem>

auto main(const int argc, const char *const *const argv) noexcept -> int {
  // init
  // modules -> ask for module arg, return enum class
  // 2 mods
  // first, module mode
  // second, project mode
}